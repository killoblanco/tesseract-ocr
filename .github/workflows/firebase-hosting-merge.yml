# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm i -g firebase-tools
      - run: npm i && npm run build
      - run: firebase use --add killoblanco-119f3 --token ${{ secrets.FIREBASE_TOKEN }}
      - run: firebase target:apply hosting tesseract tesseract
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_KILLOBLANCO_119F3 }}'
          channelId: live
          target: tesseract
          projectId: killoblanco-119f3
